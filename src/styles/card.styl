@require '_variables'
@require '_mixins'

.card
  padding 30px
  background-color $color-lightest
  box-shadow 0 1px 2px alpha($color-darkest, .1)
  border-radius(6px)
  box-sizing(border-box)
  .icon
    margin-right 10px

.card-content
  margin-top 30px
